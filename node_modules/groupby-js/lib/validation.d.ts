import { Options } from './types';
declare type Obj = {
    [key: string | number | symbol]: any;
};
export declare function validate(key: string, array: any, options?: Options<Obj, Obj>): void;
export {};
