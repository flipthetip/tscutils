"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.objectWithKeys = exports.defaultObjectWithKeysCompare = void 0;
function defaultObjectWithKeysCompare(obj, item, key) {
    return String(obj.key) === String(item[key]);
}
exports.defaultObjectWithKeysCompare = defaultObjectWithKeysCompare;
function objectWithKeys(groups, item, key, options) {
    const exists = Object.keys(groups).find((_key) => {
        return options.compare({ key: _key, value: groups[_key] }, item, key);
    });
    const index = options.formatTitle(item, key);
    if (!exists) {
        return (groups[index] = [item]);
    }
    if (options.maxByGroup && groups[index].length >= options.maxByGroup)
        return;
    groups[index].push(item);
}
exports.objectWithKeys = objectWithKeys;
