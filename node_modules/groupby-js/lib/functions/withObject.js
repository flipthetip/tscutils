"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.withObject = exports.defaultWithObjectCompare = void 0;
function defaultWithObjectCompare(obj, item, key) {
    return obj.title === item[key];
}
exports.defaultWithObjectCompare = defaultWithObjectCompare;
function withObject(groups, item, key, options) {
    const exists = groups.find((group) => {
        return options.compare(group, item, key);
    });
    if (!exists) {
        return groups.push({
            title: options.formatTitle(item, key),
            items: [item],
        });
    }
    const index = groups.indexOf(exists);
    if (options.maxByGroup && groups[index].items.length >= options.maxByGroup)
        return;
    groups[index].items.push(item);
}
exports.withObject = withObject;
