import { Options, Type, Return } from './types';
declare type Obj = {
    [key: string | number | symbol]: any;
};
/**
 * @description Função para agrupar listas de objetos
 */
export declare function groupBy<T>(key: keyof T, arrays: T[], options?: Options<Obj, T>): any;
export declare function groupBy<T, K>(key: keyof T, arrays: T[], options?: Options<Type<K, T>, T>): Return<K, T>;
export {};
